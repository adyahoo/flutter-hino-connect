part of '../../widgets.dart';

class BsSos extends StatelessWidget {
  const BsSos({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const BsNotch(),

      ],
    );
  }
}
